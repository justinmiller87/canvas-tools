.env
.env.local
.env.real
.canvas_tools_settings.json
__pycache__/
*.pyc

# Local `pip install -e .` build artifacts (see pyproject.toml)
*.egg-info/
build/
dist/
.venv/
venv/

# exports/ is a symlink into the encrypted pCloud Crypto Folder (student data).
# No trailing slash, so this matches the symlink as well as a real directory.
/exports
