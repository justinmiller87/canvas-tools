# Example input for: canvas assignments delete --course <ID> --file this.yaml
#
# Each name is re-checked against Canvas itself (not a local export) before
# anything happens — a typo or an already-deleted assignment fails loudly
# instead of silently doing nothing. Always run with --dry-run first; the
# real run still prints the full plan and requires typing "yes" before
# deleting anything.
assignments:
  - "Old Homework 1"
  - "Scrapped Quiz"
